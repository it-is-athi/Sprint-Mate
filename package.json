{
  "name": "sprint-mate",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "start": "cd frontend && npm run preview"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
